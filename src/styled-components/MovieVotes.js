import styled from 'styled-components';

const MovieVotes = styled.aside`
  background:rgba(0,0,0,.1);
  padding:5px 10px;
  display:flex;
  justify-content:flex-end;
  align-items:center;
`

export default MovieVotes;